# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CirclePercent' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CirclePercent
  pod 'Charts'
  pod 'RealmSwift'
  
  target 'CirclePercentTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CirclePercentUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
